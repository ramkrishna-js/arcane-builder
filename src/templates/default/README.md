# Arcane Bot

## Setup

1. Copy `.env.example` to `.env`
2. Set `DISCORD_TOKEN`
3. Run `arcane dev`

## Directories

- `commands/` command configs
- `events/` event configs
- `packages/` package configs
